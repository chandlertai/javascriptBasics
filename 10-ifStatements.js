// 1   //2
if (condition) { //3
// block of code to be executed if the condition is true
};

//1. Any if statement must start with the if keyword
//2. The condition that is being checked is inside the parenthesis
//3. If the condition elvaluates to true the code inside the curlu bois will execute. If it evaluates to false the code will not run.

if (10 > 5) {
    console.log("Yep, 10 is greater than 5.");
};

if (10 < 5) {
    console.log("This code will not run! 10 is not greater than 5.");
};